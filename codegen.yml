
overwrite: true
schema: http://localhost:3000/api/graphql
documents: null
generates:
  graphql/types/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
